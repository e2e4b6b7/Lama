#ifndef LAMA_RUNTIME_UTIL_H
#define LAMA_RUNTIME_UTIL_H

// CAREFUL WITH DOUBLE EVALUATION!
#define MAX(x, y) (((x) > (y)) ? (x) : (y))
#define MIN(x, y) (((x) < (y)) ? (x) : (y))

#endif //LAMA_RUNTIME_UTIL_H
